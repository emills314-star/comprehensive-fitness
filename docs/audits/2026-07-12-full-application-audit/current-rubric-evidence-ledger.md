# Current rubric evidence ledger

## Purpose and scoring neutrality

This append-only evidence snapshot supports a fresh review under `fixed-scoring-rubrics.md`. It deliberately contains no suggested category scores and no desired outcome. The revision under review is the Git commit containing this ledger; the reviewer must record its full SHA before testing and score only that same revision.

Documentation claims are routing aids, not proof. Reviewers should reproduce the referenced commands and inspect runtime behavior. Protected/private data is neither required nor permitted in a public checkout.

## Codebase evidence

| # | Category | Direct current evidence | Known limitation / independent check |
| ---: | --- | --- | --- |
| 1 | Architecture and modularity | `index.html` owns the document shell; eight responsibility-named application segments own foundation, views, analysis, workout mutations, sync/timers, submission/history, imports/readiness, and startup/events. Prescription, guided-mesocycle, rest-completion, and backup contracts remain separate modules. `test-runtime-boundary` enforces exact order, per-file parsing, combined parsing, and a 300 KiB ceiling; `sync:web`, `verify:pwa`, SW v35, and parity checks treat every public asset explicitly. | The ordered classic scripts still share a global lexical environment. Review whether that explicit, tested residual coupling is minor or whether ES-module imports are required. |
| 2 | Code quality and consistency | Canonical IDs, bounded validators, typed recommendation rejections, shared response/error wrappers, exact consent state, and one submission-acceptance boundary are covered by app-integration, schema, security, lifecycle, and runtime-boundary contracts. Static lint parses every JS/JSON/executable inline body and each runtime segment independently. | JavaScript is not statically typed; inspect implicit contracts that cross the ordered runtime segments. |
| 3 | Database design and data integrity | Versioned app/backup/prescription/mesocycle schemas; bounded migrations/imports; referential validation; immutable historical snapshots; monotonic revisions; dual-store conflict preservation; fail-closed remote deletion and recovery all have unit/browser regressions. | There is no account-backed restore. The private-only aggregate harness requires ignored local artifacts and is intentionally excluded from public CI. |
| 4 | Functional correctness and error handling | Forty-two public scripts plus mobile/desktop lifecycle suites cover success, rejection, offline, stale, duplicate, reentrant, corrupt, conflict, recovery, and runtime-boundary paths. Hosted CSP failure was detected, reproduced, fixed, and converted into a deployment regression. | Physical native/Web Push delivery remains external-device evidence, not repository proof. |
| 5 | Testing and reliability | `test:public` discovers 43 scripts and executes 42 public scripts. Candidate verification after runtime segmentation ran lint/workflow/privacy/dependency/research/PWA gates plus 222 Playwright cases: 204 passed and 18 protected/private cases intentionally skipped. The corrected consent transition also passed 20/20 repeated mobile/desktop stress cases. Runtime segmentation adds an independently parsed per-file/order/concentration regression. Recommendation fuzz executes 1,024 cases/10,240 assertions. | One private-only harness and physical-device acceptance remain outside the public checkout. Independently reproduce the complete clean committed release command rather than relying on this record. |
| 6 | Performance and efficiency | Indexed entity lookup, bounded caches, single evidence fetch, compact debounced active draft, idle full saves, progressive Templates rendering, and large-fixture browser timing are enforced. Latest focused measurements: Templates initial frame 35.6 ms mobile and 7.0 ms desktop. | Independently measure cold startup, Dashboard/Lift large fixtures, navigation, asset transfer/cache reuse, and repeated recommendation latency against documented budgets. |
| 7 | Usability and visual consistency | One tokenized design system covers five primary destinations; rich Lift/Dashboard protected baselines and mobile/desktop screen goldens pass. External-runtime extraction produced no screenshot changes. | Inspect secondary/rare error states and physical device rendering for remaining polish gaps. |
| 8 | Accessibility and responsive behavior | Axe A/AA, keyboard/focus/dialog/skip-link contracts, contextual names, announcements, 320 px, 200% equivalent zoom, dark, forced-colors, reduced-motion, touch targets, charts, and mobile/desktop viewport tests exist. | Physical screen-reader and native-device acceptance remain **NEEDS REVIEW**. |
| 9 | Documentation and developer experience | Project, architecture, decision engine, UI/UX, roadmap, push/backend, data map, setup, QA, work logs, frozen audit package, public/private test split, release commands, and privacy policy are versioned. | Cross-check every current command and explicit IMPLEMENTED/PARTIALLY IMPLEMENTED/PLANNED label; external signing/toolchain steps cannot be reproduced here. |
| 10 | Security, observability, and release readiness | Strict CSP/security headers, no executable inline app runtime, exact import boundaries, consent separation, atomic server checks, bounded deletion, public-only packaging/cache, privacy scan, immutable CI actions, dependency gates, opaque request IDs, structured allowlisted terminal events, clean-source release gate, and hosted verification evidence. | Production provider rollback controls and already-dispatched Web Push behavior require external operational review; dev dependencies retain two reported moderate findings and production has zero known vulnerabilities. |

## Workout-recommendation evidence

| # | Category | Direct current evidence | Known limitation / independent check |
| ---: | --- | --- | --- |
| 1 | Scientific evidence quality and fidelity | Research database 3.0.0 contains graded provenance, verified identifiers, rule authority, evidence/product-policy/safety distinctions, population limits, deterministic exports, and validation. | Review practical caps and readiness/deload factors for truthful product-policy labeling rather than universal scientific claims. |
| 2 | Goal alignment and program coherence | Portfolio-first mesocycles cover four program types, user-defined scope, schedule capacity, coherent sessions, sequencing, balance, frequency, sustainability caps, lifecycle/activation gates, and inspectable omissions. | Advanced per-muscle maintenance/specialization controls and revision UX remain planned refinements. |
| 3 | Correct use of personal and historical data | Comparable exposures, effort, technique, completion, pain, readiness domains, staleness, nutrition, profile fields, submitted history, optional private aggregates, conflicts, and missing data feed explicit conservative paths. Stored snapshots prevent historical reinterpretation. | Public review cannot exercise the ignored real-private aggregate fixture; use synthetic three-source fixtures and verify missing/private-unavailable behavior does not fabricate personalization. |
| 4 | Individualization and constraint satisfaction | Equipment AND/OR requirements, time/schedule capacity, scope, exclusions, exact direct target, substitutions, experience, profile goals, and audited/locked overrides have hard precedence and adversarial tests. | Review soft-preference ranking and custom-exercise reconciliation for transparent limitations. |
| 5 | Progression and autoregulation | Exact next actions cover top/back-off versus straight sets, rep loss, effort/technique validity, comparable performance, plateaus, regression, planned reductions, gaps, assisted/bodyweight direction, scoped deload, and full-program suppression. | Review missed-session and long return-to-training counterfactual breadth; no safety action may depend on a single noisy readiness marker. |
| 6 | Volume, intensity, frequency, and exercise-dose management | Versioned taxonomy distinguishes direct, fractional, incidental, and isometric roles; full-program review enforces traceable weekly dose, frequency, daily set/exercise capacity, session sustainability, and zero hidden direct credit. | Practical caps are product policy and must remain disclosed as such; independently inspect limited-schedule under-prescription behavior. |
| 7 | Fatigue, recovery, and safety handling | Illness, unsited/matching pain, invalid identity/target, unresolved taxonomy, equipment/exclusion drift, and unexpected engine faults erase executable targets. Latest/repeated pain precedes deload; readiness needs independent corroboration; mutation routes recheck safety. | Already-dispatched notifications and clinical interpretation are out of scope; app copy must remain informational and conservative. |
| 8 | Exercise selection, substitution, and taxonomy integration | All 62 canonical exercises, 66 aliases, 23 anatomical IDs, 20 programming families, 151 relationships, exact versioned direct targets, stable-ID append epochs, equipment metadata, mechanics-aware redundancy, and custom namespaces are validated end to end. | Inspect ambiguous custom identities and historical-version projection; uncertain identities must quarantine/fail closed. |
| 9 | Explainability, transparency, and user control | Immutable snapshots expose evidence/default source, constraints, changes, missing/unavailable evidence, selected/alternate candidates, volume/frequency consequences, exclusions, review blockers, and audited overrides while hard rules remain non-bypassable. | Inspect dense planner/recovery explanations for comprehension and whether every material recommendation delta is visible at the decision point. |
| 10 | Robustness, testing, monitoring, and maintainability | Unit, schema, app-integration, golden, property/fuzz, invariant, counterfactual, adversarial, determinism, namespace-collision, duplicate/transitive-exposure, legacy, profile-coherence, browser lifecycle, and hosted safety tests cover the pipeline and versioned outputs. | There is no population-level production outcome-learning service; verify that local acceptance/history feedback is sufficient for claimed behavior and that no monitoring claim exceeds privacy-safe local evidence. |

## Terminal defect gates

The implementing worker found no currently reproduced safety-critical, privacy-critical, data-loss, hard-constraint, fabricated-personalization, or unsupported-settled-science defect after the listed suites. This is not an independent clearance. A fresh reviewer must actively try to contradict it using hostile import, stale identity, offline deletion, cross-session mutation, equipment/exclusion drift, pain/illness, unknown taxonomy, deployment/CSP, cache, and large-fixture scenarios.

## Reproduction commands

```text
npm ci --ignore-scripts
npm run check:public
npm run release:verify
node scripts/test-recommendation-fuzz.js
PLAYWRIGHT_BASE_URL=<hosted-origin> npx playwright test tests/ui/safety-integrity-regressions.spec.js --workers=1
```

Record command versions, full revision SHA, skips, retries, environment limitations, and any mismatch between repository, packaged `www`, GitHub `main`, and hosted production.
